{
  "type": "module",
  "scripts": {
    "start": "pm2 start index.ts --interpreter='deno' --interpreter-args='run --allow-net --allow-read --allow-write --allow-env'"
  },
  "include": [
    "src/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^22.5.5"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "path": "^0.12.7",
    "pm2": "^5.4.2"
  }
}
